import { Routes } from '@angular/router';
import { SignalDashboardComponent } from "./signal-dashboard/signal-dashboard.component";

export const routes: Routes = [
    {
        path: 'signal-dashboard', component: SignalDashboardComponent
    },
];
